import { NextResponse, type NextRequest } from "next/server";`nimport { updateSession } from "@/lib/supabase/middleware";`n`nexport async function middleware(request: NextRequest) {`n  if (request.nextUrl.pathname.startsWith("/api/debug-auth")) {`n    return NextResponse.next();`n  }`n  return await updateSession(request);`n}`n`nexport const config = {`n  matcher: [`n    "/((?!_next/static|_next/image|favicon.ico|.*\\.(?:svg|png|jpg|jpeg|gif|webp)$).*)"`n  ],`n};
